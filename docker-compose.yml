version: '3.8'

services:
  paracetamol-rumack-matthew-nomogram:
    build: .
    container_name: paracetamol-rumack-matthew-nomogram
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=paracetamol-rumack-matthew-nomogram-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
